import * as React from 'react';

import { storiesOf } from '@storybook/react';
import KeyboardListView from '../src/components/KeyboardListView';
  
storiesOf('KeyboardListView', module)
.add('test', () => (
  <KeyboardListView />
)).add('test1', () => (
  <KeyboardListView />
)).add('test2', () => (
  <KeyboardListView />
));